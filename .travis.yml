language: swift
os: osx
osx_image: xcode9.2
xcode_project: JSONClient.xcodeproj
xcode_scheme: JSONClient
script:
    - xcodebuild -scheme JSONClient -project JSONClient.xcodeproj -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone X,OS=11.2' build test
after_success:
    - bash <(curl -s https://codecov.io/bash)

